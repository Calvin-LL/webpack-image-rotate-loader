{
  "additionalProperties": true,
  "type": "object",
  "properties": {
    "background": {
      "description": "The background color of the output image if the image is rotated by an angle other than multiples of 90. Default to `\"#000000\"`",
      "anyOf": [{ "type": "string" }, { "type": "object" }]
    },
    "angle": {
      "description": "The angle to rotate the input image. Or `undefined` which will use the EXIF `Orientation` tag. Default to `undefined`",
      "type": "number"
    }
  }
}
